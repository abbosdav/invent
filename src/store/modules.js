import { helpers } from './helpers'
import { login } from './login'
import { page } from './page'

export const modulesList = {
    modules: {
        helpers,
        login,
        page
        
    }
}